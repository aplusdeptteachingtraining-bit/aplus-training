{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/hsujyunhuei/code/aplus-training && git add -A && git diff --cached --quiet || (git commit -m \"更新(自動):Claude Code 變更同步\" && git push) 2>/dev/null || true",
            "statusMessage": "自動 commit & push 中..."
          }
        ]
      }
    ]
  }
}
